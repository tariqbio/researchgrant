# Demo Credentials

Demo seeding is enabled by default in `backend/start.sh`.
Set `SEED_DEMO_DATA=false` to disable these accounts on a real public deploy.

All demo accounts use this password:

```text
DemoPass123!
```

| Role | Email |
| --- | --- |
| God Admin | `godadmin@grantbd.test` |
| Moderator | `moderator@grantbd.test` |
| Researcher | `researcher@grantbd.test` |
| Verified Organization | `org@grantbd.test` |

Seeded sample data:

- one published organization grant
- one pending-review AI extracted draft grant
- one awarded researcher application
- one active research project
- two fund installments
- two project expenses
